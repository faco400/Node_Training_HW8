// Class representing a real user
class User {
  constructor(name, email, userId) {
    // Properties of user
    this.name = name;
    this.email = email;
    this.userId = userId;
  }
}

module.exports = User;